# v1.0.6 - Manifest Fix

Correção:
- `fx_version '1.0.5'` -> `fx_version 'cerulean'`
- `version '1.0.6'`

Também remova/comente `ensure [3^Nativos]` do server.cfg.
A pasta contém apenas RDR3natives, que não é resource RedM.

Corrija ainda:
```
ensure 
vorp_lootnpcs
```
para:
```
ensure vorp_lootnpcs
```
