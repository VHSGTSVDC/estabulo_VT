# v3.0.1 HOTFIX — Cavalo invisível

Correções:
- aguarda o PED estabilizar antes de aplicar acessórios;
- restaura `visible`, alpha e LOD após spawn e acessórios;
- monitor leve detecta `IsEntityVisible=false` ou alpha inválido;
- primeiras falhas são corrigidas sem respawn;
- se a entidade continuar quebrada, recria somente o PED local;
- respawn seguro reutiliza o mesmo horse ID/dados;
- não cria registro MySQL, stash ou cavalo duplicado;
- desmonta o jogador antes do fallback de respawn;
- cooldown de reparo evita loop de respawn.

Fallback manual:
`/corrigircavalo`

Menu do botão direito:
`👁️ Corrigir visual`

Não requer SQL novo.
