# v1.1.0 — Reparo completo de schema

Erro confirmado:
`Unknown column 'is_primary' in 'field list'`

A tabela `lrrp_horses` da base é anterior ao schema atual.

Execute UMA VEZ:
`sql/REPAIR_SCHEMA_COMPLETO_V1.1.0.sql`

O script usa `ADD COLUMN IF NOT EXISTS` e não apaga cavalos.

Depois:
`restart estabulo_VT`

A compra continua com proteção:
- valida saldo;
- cobra;
- tenta INSERT;
- se o INSERT falhar, reembolsa automaticamente.
