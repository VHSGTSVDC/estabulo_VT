# v1.1.7 — Tecla H para assobiar

- Mantém `/assobiar` funcionando.
- H usa diretamente `INPUT_WHISTLE` (`0x24978A28`).
- Não usa `RegisterKeyMapping`.
- H e `/assobiar` executam exatamente a mesma função.
- Cooldown de 1 segundo para evitar chamadas duplicadas.
- Log no F8:
  - `TECLA H ATIVA`
  - `ASSOBIO H | solicitando cavalo principal`
  - `ASSOBIO H | chamando cavalo ativo`

Não requer SQL.
