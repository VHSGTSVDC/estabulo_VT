# v1.0.5 - Database Repair

Erro corrigido:
`Unknown column 'birth_at' in 'field list'`

Causa: tabela `lrrp_horses` de uma versão antiga sem as migrações v0.5+.

## Atualização
Execute UMA VEZ:
`sql/REPAIR_SCHEMA_V1.0.5.sql`

O script usa `ADD COLUMN IF NOT EXISTS` e `CREATE TABLE IF NOT EXISTS`.
Ele não apaga os cavalos existentes.

Depois:
`restart estabulo_VT`
