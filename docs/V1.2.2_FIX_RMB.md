# v1.2.2
Correção do menu de cuidados no botão direito:
- captura `IsDisabledControlJustPressed` e `IsControlJustPressed`;
- desabilita o RMB do jogo apenas quando estiver próximo do próprio cavalo;
- evita conflito com o menu nativo "Ações";
- NUI ganha foco e cursor;
- ESC fecha o menu;
- log: `MENU CAVALO ABERTO`.
Não requer SQL.
